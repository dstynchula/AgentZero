---
description: Interactive AgentZero MCP lead session — always confirm with user before scrape/commit
alwaysApply: true
---

# AgentZero MCP (interactive chat)

When AgentZero MCP tools are available, run the **lead session in chat**:

1. `lead_session_workflow` or `suggest_targets` → confirm titles/comp with the user
2. `check_sessions` → fix login/CAPTCHA if needed (Chrome CDP auto-starts)
3. `run_scrape` → only after explicit user OK
4. Present leads → user picks job_ids
5. `commit_leads` → only selected ids (SQLite only; web UI for tracking)

Never batch-scrape or promote leads without user confirmation in the conversation.
Point operators to `docker compose up web` → http://localhost:8080 for day-to-day tracker edits.
